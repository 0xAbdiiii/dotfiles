general {
    lock_cmd = pidof hyprlock || hyprlock        # avoid starting multiple hyprlock instances.
    before_sleep_cmd = loginctl lock-session     # lock before suspend.
    after_sleep_cmd = hyprctl dispatch dpms on   # to avoid having to press a key twice to turn on the display.
}

listener {
    timeout = 180                                # 3min (Dim monitor)
    on-timeout = brightnessctl -s set 10%        # set monitor backlight to minimum
    on-resume = brightnessctl -r                 # monitor backlight restore
}

listener {
    timeout = 210                                # 3.5min (Lock screen)
    on-timeout = loginctl lock-session           # lock screen when timeout has passed
}

listener {
    timeout = 300                                # 5min (Screen off)
    on-timeout = hyprctl dispatch dpms off       # screen off when timeout has passed
    on-resume = hyprctl dispatch dpms on         # screen on when activity is detected
}

listener {
    timeout = 1800                               # 30min (Suspend)
    on-timeout = systemctl suspend               # suspend pc
}
